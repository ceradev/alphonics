import React from 'react';
import Layout from '../layouts/Layout';

const Home = () => {
    return (
        <Layout>
            <h1>Hello, you are on the home</h1>
        </Layout>
    );
};

export default Home;